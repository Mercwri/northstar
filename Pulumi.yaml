name: northstar
description: self hosting northstar with some customizations
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-typescript
